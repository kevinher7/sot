# Task

Investigate why the インターン (intern) mode cannot be clicked/switched to in Google Chrome. Look for the UI component that renders the intern mode toggle/switch, any click handlers, CSS issues (like pointer-events, z-index, overflow hidden), and any browser-specific styling that might prevent interaction in Chrome. This is a Chrome extension project.
